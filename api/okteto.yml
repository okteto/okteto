name: api
image: okteto/golang:1.12
workdir: /app
command: ["bash"]
forward:
  - "8080:8080"
resources:
  requests:
    cpu: "500m"
volumes:
  - name: go-pkg
    path: /go/pkg/
  - name: go-cache
    path: /root/.cache/go-build/

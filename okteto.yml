dev:
  cli:
    image: okteto/golang:1
    command: bash
    workdir: /okteto
    volumes:
      - /go/pkg/
      - /root/.cache/go-build/
    autocreate: true
    persistentVolume:
      size: 5Gi
    
    

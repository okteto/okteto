# Add any directories, files, or patterns you don't want to be tracked by version control

# macOS
.DS_Store

# IDEs
.vscode
.idea
.atom

# VSCode Plugins
.devcontainer

# Golang binary
cli
.syncthing*
bin/*
coverage.txt
coverage.html
dist

# don't add the binary created by running go build from the root of the repo
/okteto

# lint
golangci-report.xml

node_modules

# Claude local config (personal, not shared)
# tasks.local is listed separately: the glob above needs a dot after "local",
# so it does not match a directory named tasks.local.
.claude/*.local.*
.claude/tasks.local/
.claude/worktrees/

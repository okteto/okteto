comment:
  layout: header, changes, diff, sunburst

coverage:
  status:
    project:
      default:
        target: auto
        threshold: '15%'

    patch:
      default:
        target: 75%
        only_pulls: true

ignore:
  - '**/*_test.go'
  - integration/*
  - scripts/*
  - internal/*

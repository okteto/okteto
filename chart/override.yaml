githubAuth:
  clientId: "47867be52b46a2d9d302"
  clientSecret: "9afa94d61dfac781d18ecc5c49cdfccb61d024a5"

images:
  api:
    repository: gcr.io/free-tier-219708/api
    tag: latest
  frontend:
    repository: gcr.io/free-tier-219708/frontend
    tag: latest
  pullPolicy: Always

clusterPublicEndpoint: "35.204.232.187"

ingress:
  enabled: true
  annotations:
    kubernetes.io/ingress.class: nginx
    kubernetes.io/tls-acme: "true"
  path: /
  hosts:
    - cloud.usa.okteto.net
  tls: 
    - secretName: okteto
      hosts:
        - cloud.usa.okteto.net

## Install Default RBAC roles and bindings
rbac:
  install: true

resources: {}
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  # limits:
  #  cpu: 100m
  #  memory: 128Mi
  # requests:
  #  cpu: 100m
  #  memory: 128Mi
